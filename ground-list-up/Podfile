# Uncomment the next line to define a global platform for your project
platform :ios, '16.1'

target 'ground-list-up' do
  use_frameworks!
  # Comment the next line if you don't want to use dynamic frameworks
  pod 'Alamofire'
  # Pods for ground-list-up

  target 'ground-list-upTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ground-list-upUITests' do
    # Pods for testing
  end

end
